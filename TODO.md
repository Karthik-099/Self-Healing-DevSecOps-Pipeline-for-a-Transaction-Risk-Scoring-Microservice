# TODO - Splunk + Simulation + README update

- [ ] Gather repo understanding (done: inspected key files: README, Docker Compose, app, SPL query, Fluent Bit config, rollback handler)
- [ ] Add missing Splunk simulation assets: dummy data generator + Splunk saved-search evaluator + sample alert payload to rollback handler
- [ ] Add docs for local simulation flow (run replay generator, inspect results JSON, optionally call rollback handler)
- [x] Update README.md with the new simulation steps (replay generator + webhook payload)
- [x] Run quick smoke tests (unit tests + start stack locally + curl/simulate) if feasible
- [ ] (Optional) Git commands + branch for changes

